name: astrbot_plugin_yt_dlp # 插件唯一识别名，以 astrbot_plugin_ 前缀开头
# 插件名称（给人看的）
display_name: 全能视频直链提取器
# 描述
description: 基于本地 yt_dlp 的全能视频解析插件，支持油管、B站等。
# 版本号
version: 4.5.1
# 作者
author: ハ七
# 插件的仓库地址
# 显式指定 master：AstrBot 获取 GitHub 默认分支失败时会回退到 main，
# 但本仓库的默认分支名为 master，会导致插件更新下载 main.zip 返回 404。
repo: "https://github.com/204343414/astrbot_plugin_yt_dlp/tree/master"

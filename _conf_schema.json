{
  "proxy": {
    "description": "海外视频代理",
    "type": "object",
    "items": {
      "enabled": {
        "description": "启用代理",
        "type": "bool",
        "default": false
      },
      "url": {
        "description": "代理地址",
        "type": "string",
        "default": "http://127.0.0.1:7890"
      }
    }
  },
  "download": {
    "description": "单视频下载与上传限制",
    "type": "object",
    "items": {
      "max_quality": {
        "description": "最高画质",
        "type": "string",
        "default": "1080p",
        "options": ["480p", "720p", "1080p", "1440p", "2160p", "最高画质"]
      },
      "max_size_mb": {
        "description": "通用下载文件大小上限MB",
        "type": "int",
        "default": 100
      },
      "qq_official_max_size_mb": {
        "description": "QQ官方本地Base64上传安全上限MB，默认70；最高允许配置100。超过时在上传前停止，避免413重试风暴",
        "type": "int",
        "default": 70
      },
      "auto_delete_seconds": {
        "description": "上传后临时文件保留秒数",
        "type": "int",
        "default": 300
      },
      "prefer_h264": {
        "description": "优先H.264，提高QQ客户端兼容性",
        "type": "bool",
        "default": true
      },
      "max_concurrent": {
        "description": "同时下载任务数，建议公家Bot保持1",
        "type": "int",
        "default": 1
      }
    }
  },
  "ffmpeg": {
    "description": "FFmpeg设置",
    "type": "object",
    "items": {
      "use_imageio": {
        "description": "优先使用imageio-ffmpeg",
        "type": "bool",
        "default": true
      },
      "custom_path": {
        "description": "自定义FFmpeg路径，留空自动寻找",
        "type": "string",
        "default": ""
      }
    }
  },
  "access_control": {
    "description": "字幕组操作员",
    "type": "object",
    "items": {
      "operator_openids": {
        "description": "允许下载的QQ官方用户OpenID，多个用逗号或换行分隔；AstrBot管理员自动允许",
        "type": "text",
        "default": ""
      },
      "allowed_qqofficial_instance_ids": {
        "description": "允许下载的QQ官方平台实例ID，例如 default_102824564 或 102824564。注意：这是Bot实例/AppID范围，会放行该实例下所有群，不是群号。多个用逗号或换行分隔",
        "type": "text",
        "default": ""
      }
    }
  },
  "moderation": {
    "description": "国外视频标题、描述和封面强制审核；国内平台默认免审",
    "type": "object",
    "items": {
      "provider_id": {
        "description": "多模态审核Provider；留空使用当前会话Provider，无可用Provider时拒绝",
        "type": "string",
        "default": "",
        "_special": "select_provider"
      },
      "group_openid_whitelist": {
        "description": "下载与审核白名单群的group_openid，多个用逗号或换行分隔；不是普通数字群号。拒绝提示会显示当前群的真实group_openid，复制填入即可",
        "type": "text",
        "default": ""
      }
    }
  }
}
